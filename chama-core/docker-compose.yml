version: '3.8'
services:
  postgres:
    image: postgres:latest
    container_name: chama_postgres
    environment:
      POSTGRES_USER: felixtwoli
      POSTGRES_PASSWORD: Twoli8451#
      POSTGRES_DB: chama_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

